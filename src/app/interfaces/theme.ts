export interface Theme {
  light: string;
  normal: string;
  dark: string;
  accent: string;
  background: string;
  onLight: string;
  onNormal: string;
  onDark: string;
  onAccent: string;
  onBackground: string;
}
